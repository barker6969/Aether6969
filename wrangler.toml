# Cloudflare Pages — static download site (download/)
# Deploy: npx wrangler pages deploy download --project-name=aether-repair-download
# Or via GitHub Action: .github/workflows/cloudflare-pages.yml

name = "aether-repair-download"
compatibility_date = "2025-08-01"

# Pages output directory (CLI still passes the path; this documents intent)
pages_build_output_dir = "download"
